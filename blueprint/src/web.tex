% This file makes a web version of the blueprint
% It should include all the \usepackage needed for this version.
% The template includes standard AMS packages.
% It is otherwise a very minimal preamble (you should probably at least
% add cleveref and tikz-cd).

\documentclass{report}

\usepackage{amssymb, amsthm, amsmath}
\usepackage{hyperref}
\usepackage[showmore, dep_graph]{blueprint}


\input{macros/common}
\input{macros/web}

\home{https://DavidLedvinka.github.io/MeanValueInequality}
\github{https://github.com/DavidLedvinka/MeanValueInequality}
\dochome{https://DavidLedvinka.github.io/MeanValueInequality/docs}

\title{The Mean Value Inequality}
\author{David Ledvinka}

\begin{document}
\maketitle
\input{content}

\begin{document}
\maketitle
\input{content}

%\begin{definition}
%    \label{def:myDef}
%    \lean{PolygonalLine}
%    \leanok
%    This is the definition of a polygonal line segment.
%\end{definition}

% \begin{theorem}
%   \label{thm:my_theorem1}
%   \lean{my_theorem1}
%   \leanok
%   \uses{def:myDef}
%   This is a theorem. It uses def! It is stated and proved in Lean!
% \end{theorem}

% \begin{proof}
%   \leanok
%   This is the proof of my theorem.
% \end{proof}

% \begin{theorem}
%   \label{thm:my_theorem2}
%   \lean{my_theorem2}
%   \leanok
%   \uses{def:myDef}
%   This is a theorem. It uses def! It is stated in Lean but not proved in Lean.
% \end{theorem}

% \begin{proof}
%   This is the proof of my theorem.
% \end{proof}

% \begin{theorem}
%   \label{thm:my_theorem3}
%   \lean{my_theorem3}
%   \uses{def:myDef}
%   This is a theorem. It uses def! It is not stated or proved in Lean.
% \end{theorem}

% \begin{proof}
%   This is the proof of my theorem.
% \end{proof}
\begin{theorem}
    \label{thm:existence of polygonal line}
    \lean{polygonal_connected_of_connected}
    \uses{lem:V is open}
    \uses{lem:V is closed}
    \uses{def:polygonalLine}
    \leanok
    if $U$ is an open connected set of a normed vector space, then any two points of $U$ can be joined by a polygonal line in $U$.
\end{theorem}

\begin{proof}
\leanok
% TODO: Add proof to latex
\end{proof}

\begin{lemma}
    \label{lem:V is open}
    \uses{def:polygonalLine}
    let $x \in U$, and $V$ the set of all points in $U$ that can be joined to $x$ by a polygonal line contained in $U$. Then $V$ is open.
\end{lemma}
\begin{proof}
    let $a \in V$ be the end of the polygonal line $l$ which starts from $x$.\\ Since $U$ is open, there exist a ball $B(a,r)$ contained in $U$. Every points $p \in B(a,r)$ can be joined to $a$ by a straight line. Therefore, by joining the two polygonal line, we have a polygonal line joining $x$ and $p$. Hence, $B(a,r) \subset V$ and $V$ is open.
\end{proof}
\begin{lemma}
    \label{V is closed}
    \uses{def:polygonalLine}
    $V$ is closed in $U$.
\end{lemma}
\begin{proof}
    Let $a \in \overline{V}$, Since $U$ is open, there exists a ball $B(a,r)$ contained in $U$. Since $a$ is in the closure of $V$, there exists a point $b \in B(a,r) \cap V$. Then $a$ can be joined to $b$ via a straight line contained in $U$ and $b$ can be joined to $x$ by a polygonal line in $U$. Therefore, $a$ can be joined to $x$ by a polygonal line in $U$. Hence $a \in V$ and $V$ is closed in $U$.
\end{proof}
\begin{proof}[Proof of Theorem \ref{thm:existence of polygonal line}]
    Since $V$ is both open and closed in $U$, and $U$ is connected then either $V = U$ or $V = \emptyset$. If $U$ is not empty then $V$ contained $x$ and is not empty, Hence $V = U$.\\ Otherwise if $U = \emptyset$, then the result is trivial.
\end{proof}
\begin{definition}
    \label{def:distance in U}
    \uses{thm:existence of polygonal line}
    Let $U$ be an open connected set of a Banach space $E$. for $a, b \in U$, define $d_U(a,b)$ to be the infimum of the lengths of the polygonal lines contained in $U$ joining $a$ and $b$.
\end{definition}

\begin{theorem}
    \label{thm:d_U is a distance}
    \uses{def:distance in U}
    $d_U(a,b)$ is a distance in the topological space $U$.
\end{theorem}
\begin{proof}

\end{proof}
\begin{theorem}
    \label{thm:mean value theorem in connected open set}
    \uses{def:distance in U}
    \uses{thm:existence of polygonal line}
    Let $U$ be a connected open set of a Banach space $E$. Let $f: U \rightarrow F$ be a differentiable mapping with values in a Banach space $F$. Assume that $$||f'(x)|| \leq k, \forall x \in U$$
    Then, for any $x_1,x_2 \in U$, $||f(x_1)-f(x_2)|| \leq k \cdot d_U(x_1,x_2)$.
\end{theorem}
\begin{proof}
    Let $x_1,x_2 \in U$ and $l$ a polygonal line contained in $U$ joining $x_1$ and $x_2$. then let $l$ be composed of $n$ line segments with endpoints $(a_0,a_1), (a_1,a_2), \dots, (a_{n-1},a_n)$, where $a_0=x_1, a_n = x_2$ and $a_i \in U$. Then from the mean value inequality on each interval $(a_i, a_{i+1})$, we have $||f(a_i)-f(a_{i+1})|| \leq k \cdot ||a_i-a_{i+1}||$. From the triangle inequality we have $$||f(x_1)-f(x_2)|| \leq \sum_{i=0}^{n-1} ||f(a_i)-f(a_{i+1})|| \leq \sum_{i=0}^{n-1} k \cdot ||a_i-a_{i+1}|| = k \cdot \text{length}(l)$$ as $\text{length}(l)$ is the sum of the length of each interval. Therefore, taking the infimum we get, $$||f(x_1)-f(x_2)|| \leq k \cdot \inf{\text{length}(l)} = k\cdot d_U(x_1,x_2)$$
\end{proof}


\begin{definition}
    \label{def:path}
    In a topological space $X$ a continuous mapping $\varphi \colon [0,1] \to X$ is called a \emph{path}; the point $\varphi(0)$ is called the \emph{origin of the path} and the point $\varphi(1)$ is called the \emph{end of the path}. Two points $a,b \in X$ can be joined by a path if there exists $\varphi$ such that $\varphi(0) = a$ and $\varphi(1) = b$
\end{definition}

\begin{definition}
    \label{def:polygonalLine}
    Let $(E, ||\cdot||)$ be a normed vector space over $\mathbb{R}$, with subset $A \subseteq E$. A \emph{polygonal line} in $A$ is a path $\varphi \colon [0,1] \to A$ such that there exists $n \geq 1$ with points $t_0,\dots,t_n$ on the interval $[0,1]$:
    \begin{center}
    $t_0 = 0 < t_1 < \dotsb < t_{n-1} < t_n = 1$
    \end{center}
    so that for each $i \in \{0,\dots,n-1\}$, the restriction of $\varphi$ on the interval $[t_i,t_{i+1}]$ is an affine map. This is to say that $t \in [t_i,t_{i+1}] \implies \varphi(t) = t \cdot v_i + c_i$ for some $v_i,c_i \in E$. Hence the image of $[t_i,t_{i+1}]$ in $\varphi$ is a straight-line segment in $A$ from $\varphi(t_i)$ to $\varphi(t_{i+1})$.
\end{definition}

\begin{definition}
    \label{def:intervalLength}
    Let $(E, ||\cdot||)$ be a normed vector space over $\mathbb{R}$. The \emph{length of an interval} with origin $a \in E$ and end $b \in E$ is $d(a,b) := ||b - a||$.
\end{definition}

\begin{definition}
    \label{def:polygonalLineLength}
    Given a partition $(t_i)_{i=0}^{n}$ for some fixed $n \geq 1$, the \emph{length of a polygonal line} $\varphi$ respecting this partition is the sum of the lengths of the intervals from which it is formed:
    \begin{center}
    $L((t_i), n, \varphi) := \sum_{i=0}^{n-1}||\varphi(t_{i+1}) - \varphi(t_i)||$
    \end{center}
\end{definition}

\begin{lemma}
    \label{lemma:norm_sum_at_least_sum_norm}
    Given a sequence $(a_i)_{i=0}^{n}$ with $a_i \in E$ for some $n \geq 1$, $\sum_{i=0}^{n-1}||a_{i+1} - a_i|| \geq ||a_n - a_0||$
\end{lemma}

\begin{proof}[Proof of Lemma \ref{lemma:norm_sum_at_least_sum_norm}]
    \begin{itemize}
        \item \textbf{Base Case ($n=1$):}
        For $n=1$, we need $||a_1 - a_0|| \geq ||a_1 - a_0||$ which follows immediately by reflexivity.

        \item \textbf{Induction Hypothesis:}
        Assume that the result holds for any such sequence $(b_i)_{i=0}^k$. That is,
        \begin{center}
            $\sum_{i=0}^{k-1}||b_{i+1} - b_i|| \geq ||b_k - b_0||$
        \end{center}

        \item \textbf{Induction Step:}
        We want to show that $\sum_{i=0}^{k}||a_{i+1} - a_i|| \geq ||a_{k+1} - a_0||$ must follow from this assumption, for some arbitrary sequence  $(a_i)_{i=0}^{k+1}$.

        $||a_{k+1} - a_0|| =  ||(a_{k+1} - a_k) + (a_k - a_0)|| \\ \leq ||a_{k+1} - a_k|| + ||a_k - a_0|| \\ \leq ||a_{k+1} - a_k|| + \sum_{i=0}^{k-1}||a_{i+1} - a_i|| \\ = \sum_{i=0}^{k}||a_{i+1} - a_i||$
    \end{itemize}

    Therefore, by the principle of mathematical induction, $\sum_{i=0}^{n-1}||a_{i+1} - a_i|| \geq ||a_n - a_0||$ for any such sequence with any $n \geq 1$.
\end{proof}

\begin{theorem}
\label{thm:polygonal_line_length_at_least_interval_length}
    % \uses{def:polygonalLineLength}
    % \uses{def:intervalLength}
    % \uses{lemma:norm_sum_at_least_sum_norm}
    The length of a polygonal line $\varphi$ respecting a partition $(t_i)_{i=0}^{n}$ for some fixed $n \geq 1$ between origin $a = \varphi(0)$ and end $b = \varphi(1)$ is at least equal to the length of the interval between these points:
    \begin{center}
    $\sum_{i=0}^{n-1}||\varphi(t_{i+1}) - \varphi(t_i)|| \geq ||\varphi(1) - \varphi(0)||$
    \end{center}
\end{theorem}

\begin{proof}[Proof of Theorem \ref{thm:polygonal_line_length_at_least_interval_length}]
    We consider the sequence of points $(p_i)_{i=0}^n$ with $p_i := \varphi(t_i)$. Then by the previous lemma, with $p_0 = \varphi(0)$ and $p_n = \varphi(1)$, our desired result follows immediately.
\end{proof}
\end{document}
